package uz.nusratedu.user;

/**
 * ✅ NO CHANGES: UserRole enum
 *
 * This simple enum requires no conversion.
 * Used to define user roles in the system.
 */
public enum UserRole {
    USER,
    ADMIN
}